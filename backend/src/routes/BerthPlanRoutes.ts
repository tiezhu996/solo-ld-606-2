import { Router } from "express"; import { berthPlanController } from "../controllers/BerthPlanController"; const router = Router(); router.get("/", berthPlanController.list); router.post("/", berthPlanController.create); router.post("/precheck", berthPlanController.precheck); router.put("/:id", berthPlanController.update); router.post("/:id/approve", berthPlanController.approve); export default router;
